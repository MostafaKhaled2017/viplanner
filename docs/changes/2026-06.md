# 2026-06 Changes

## 2026-06-03 13:30 - Multi-environment reconstruction and cost-map configs

**Change size**

- `M`

**Files changed**

- `viplanner/config/costmap_cfg.py`
- `viplanner/depth_reconstruct.py`
- `viplanner/cost_builder.py`
- `viplanner/config/costmap.yaml`
- `tests/test_reconstruction_cfg.py`
- `tests/test_robot_height_cfg.py`
- `tests/test_cost_builder.py`
- `tests/test_debug_project_cloud_to_images.py`
- `commands.bash`
- `docs/build_and_run.md`
- `docs/current_state.md`
- `docs/important_files.md`
- `docs/changes/2026-06.md`

**What changed**

- Replaced reconstruction config `env` usage with validated `env_list`.
- Added single-environment reconstruction config views for per-environment processing.
- Updated depth reconstruction and cost-map build CLIs to process each `env_list` entry separately and stop on first failure.
- Updated cost-map root path resolution so `config.general.root_path` is treated as the parent directory containing environments.
- Updated tests, command notes, example config, and documentation for the new config shape.

**Context**

- Reconstruction and cost-map scripts previously handled one environment at a time.
- The requested behavior is to run the same workflow independently for each environment listed in the config.

**Impact**

- Existing configs using `reconstruction.env` now fail with a migration message and must use `reconstruction.env_list`.
- Cost-map configs should set `config.general.root_path` to the parent data directory, or leave it unset/null to derive from `reconstruction.data_dir`.

**Validation**

- Command: `python -m py_compile viplanner/config/costmap_cfg.py viplanner/depth_reconstruct.py viplanner/cost_builder.py viplanner/debug_project_cloud_to_images.py`
- Result: Passed.

- Command: `pytest tests/test_reconstruction_cfg.py tests/test_robot_height_cfg.py tests/test_cost_builder.py`
- Result: 24 passed. Pytest reported workspace `.pytest_cache` write warnings.

- Command: `pytest tests/test_debug_project_cloud_to_images.py`
- Result: 14 passed. Pytest reported workspace `.pytest_cache` write warnings.

- Command: `pytest tests/test_reconstruction_cfg.py tests/test_robot_height_cfg.py tests/test_cost_builder.py tests/test_debug_project_cloud_to_images.py`
- Result: 38 passed. Pytest reported workspace `.pytest_cache` write warnings.

**Notes**

- Full training, simulation, and reconstruction jobs were not run.

## 2026-06-03

### Documentation

- Created the initial `docs/` project wiki structure required by the repository instructions.
- Added overview, architecture, setup, build/run, dependency, current-state, known-issue, decision, next-task, glossary, ROS, and launch documentation pages.

### Validation

- Command: `sed -n '1,220p' /root/.codex/AGENTS.md`
- Result: Confirmed required `docs/` structure and ROS-specific documentation expectations.

- Command: repository inspection with `find`, `sed`, `rg --no-ignore`, and `git status --short`
- Result: Identified project structure, Python package metadata, ROS packages, launch files, configs, topics, and existing unrelated working-tree changes.

- Command: `find docs -maxdepth 2 -type f | sort`
- Result: Confirmed the required documentation pages and change-log files exist.

- Runtime tests: Not run. This was a documentation-only change.
