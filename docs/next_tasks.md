# Next Tasks

- Fix the malformed `.gitignore` pattern `AGENTS.md\` if it is not intentional.
- Decide whether generated local files such as screen captures and render option JSON should be ignored or committed.
- Confirm whether deleted tracked `docs/example_carla.png` and `docs/example_matterport.png` should be restored, replaced, or removed from version control.
- Run a focused test pass after code changes, for example `pytest tests/test_cost_builder.py tests/test_reconstruction_cfg.py tests/test_sem_cost_map.py`.
- Update `docs/current_state.md` after any meaningful build, test, training, or validation milestone.
- Expand ROS topic documentation if new launch configs or robot-specific topic mappings are added.
- Keep config docs synchronized when defaults in YAML or config classes change.
